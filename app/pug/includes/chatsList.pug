ul(class="list-group mt-5")
  each persons, index in chatsList
    each person in persons
      li(class="list-item list-unstyled action-on-chat")
        a(href="#" class="list-group-item-action d-flex text-decoration-none px-3 shadow-sm border-top-0 action-on-link")
          img.avatar(src= person.avatar)  
          div(class="d-flex flex-column p-4")
            div(class="d-flex justify-content-between w-custom")
              h3(class="fw-bold text-dark h6")= person.name
              p(class="small text-secondary")=person.time
            p(class="small text-secondary overflow-hidden text-truncate w-custom")= person.message